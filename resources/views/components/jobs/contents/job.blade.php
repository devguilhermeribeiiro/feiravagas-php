<section class="max-w-3xl mx-auto space-y-6 leading-relaxed">
    <h2 class="text-3xl flex flex-col items-start justify-center font-semibold">{{ $job->title }}</h2>

    <p class="font-normal text-2xl">{{ $job->company }}</p>

    <p class="text-xl font-light">{{ $job->description }}</p>
</section>
